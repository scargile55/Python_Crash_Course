def make_shirt(size, text='Gucci'):
    """Prints statement with a a default paramater"""
    print(f"We only have a size {size} in the {text.title()} shirt.")

# Calling funtion with an argument
make_shirt('medium')